# ===============================
# = LOGGING
# ===============================

logging.level.root=INFO
logging.level.org.springframework=ERROR
logging.level.com.freedom.live=DEBUG
 
#output to a temp_folder/file
logging.file=application-live-option-data-sb.log

# ===============================
# = DATA SOURCE
# ===============================

# Set here configurations for the database connection
spring.datasource.url = jdbc:mysql://freedom-db.ctchbk7wuttp.us-east-2.rds.amazonaws.com:3306/engine_live?useSSL=false
spring.datasource.username = root09
spring.datasource.password = ashish123

# ===============================
# = JPA SETTINGS
# ===============================

# Hibernate ddl auto (create, create-drop, update)
## spring.jpa.hibernate.ddl-auto = create

# Use Jadira Usertype for mapping Joda Time types
# For Hibernate native properties must be used spring.jpa.properties.* (the 
# prefix is stripped before adding them to the entity manager).
spring.jpa.properties.jadira.usertype.autoRegisterUserTypes = true

# spring.jpa.properties.hibernate.show_sql=false
# spring.jpa.properties.hibernate.use_sql_comments=true
# spring.jpa.properties.hibernate.format_sql=true
 
 # spring.jpa.properties.hibernate.type=trace
 # spring.jpa.show-sql=true
 # logging.level.org.hibernate=TRACE


 # hibernate.jdbc.batch_size=20
 # hibernate.order_inserts=true
 # hibernate.order_updates=true
 # hibernate.jdbc.batch_versioned_data=true

# spring.jpa.hibernate.jdbc.batch_size=20
# spring.jpa.hibernate.order_inserts=true
# spring.jpa.hibernate.order_updates=true
# spring.jpa.hibernate.jdbc.batch_versioned_data=true

 spring.jpa.properties.hibernate.jdbc.batch_size=20
 spring.jpa.properties.hibernate.order_inserts=true
 spring.jpa.properties.hibernate.order_updates=true
 spring.jpa.properties.hibernate.jdbc.batch_versioned_data=true

# scrapping.debug - used to run scrapping even after market hours 
scrapping.debug=true

